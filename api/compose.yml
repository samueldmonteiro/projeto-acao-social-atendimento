
services:
  postgres:
    image: postgres:16
    container_name: postgres-atendimento
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_DATABASE}
    volumes:
      - pgdata_atendimento:/var/lib/postgresql/data

volumes:
  pgdata_atendimento: